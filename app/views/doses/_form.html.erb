<h1>Create a recipe for your cocktail</h1>
<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.input :ingredient, :collection => Ingredient.all.to_a %>
  <%= f.input :description %>
  <%= f.input :ingredient, :collection => Ingredient.all.to_a %>
  <%= f.input :description %>
  <%= f.input :ingredient, :collection => Ingredient.all.to_a %>
  <%= f.input :description %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>



